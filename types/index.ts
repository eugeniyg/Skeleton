export interface IBrowserLanguage {
  code: string,
  script: null|string,
  region: undefined|string,
  quality: number,
}

export interface IPhoneCode {
  countryCode: string,
  code: string,
  mask?: string,
  value?: string,
}

